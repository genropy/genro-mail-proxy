[smtp]
host = smtp.gmail.com
port = 587
user = user@example.com
password = secret
[fetch]
url = http://localhost:8080/pending-emails
[storage]
db_path = /tmp/mail_service.db
[server]
# host = 0.0.0.0
# port = 8000
# api_token = your-api-token
sync_token = your-sync-token
[sync]
# Endpoint invoked by the dispatcher to deliver batched reports
proxy_sync_url = http://proxy.example.com/mail/proxy_sync
proxy_sync_user = syncuser
proxy_sync_password = syncpass
# Optional: limit of delivery report entries per request
# proxy_sync_batch_size = 100
